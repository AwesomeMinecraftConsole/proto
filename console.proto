syntax = "proto3";
package com.uramnoil.awesome_minecraft_console.protocols;

service Console {
  rpc Command (Empty) returns (stream CommandResponse);
  rpc Lines (stream LineRequest) returns (Empty);
}

message CommandResponse {
  string message = 1;
}

message LineRequest {
  string message = 1;
}

message Empty {}